import { keyframes } from '@chakra-ui/react';

// Constants about animation for alerts

export const animationKeyframes = keyframes`
    form {margin-right: -100vw;} 
    to {margin-right: 0}
`;

export const animation = `${animationKeyframes} 2s`;
